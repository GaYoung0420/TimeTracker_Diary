# 투두 카테고리 기능 사용 가이드

## 개요

투두 리스트에 카테고리 기능이 추가되어, 투두를 이벤트 카테고리와 연결하고 완료 시 자동으로 이벤트를 생성할 수 있습니다.

## 주요 기능

### 1. 투두 카테고리 생성
- "투두 카테고리 관리" 버튼을 클릭
- 카테고리 이름 입력 (예: "개인", "업무", "학습")
- 연결할 이벤트 카테고리 선택 (예: "④ 영적")
- 저장

### 2. 투두 생성 시 설정
- 할 일 입력
- **투두 카테고리 선택**: 생성한 투두 카테고리 중 하나 선택
- **시작 시간 입력**: 언제 시작할 계획인지
- **소요 시간 입력**: 몇 분이 걸릴지 (분 단위)
- 추가 버튼 클릭

### 3. 타임라인에 투두 계획 표시
- 시작 시간과 소요 시간이 입력된 투두는 자동으로 타임라인의 "계획" 열에 표시됩니다
- 파란색 점선 테두리로 구분됩니다
- 📋 아이콘이 표시됩니다

### 4. 투두 완료 시 이벤트 자동 생성
- 투두 카테고리가 이벤트 카테고리와 연결된 경우
- 투두를 완료 체크하면
- 연결된 이벤트 카테고리로 실제 이벤트가 자동 생성됩니다
- 타임라인의 "실제" 열에 표시됩니다

## 사용 예시

### 예시 1: 영적 활동 관리
1. 투두 카테고리 "개인" 생성 → "④ 영적" 이벤트 카테고리 연결
2. 할일: "성경 읽기"
   - 투두 카테고리: 개인
   - 시작 시간: 07:00
   - 소요 시간: 30분
3. 타임라인 "계획" 열에 07:00-07:30 표시
4. 완료 체크 → "④ 영적" 카테고리로 이벤트 생성

### 예시 2: 업무 관리
1. 투두 카테고리 "업무" 생성 → "① 업무" 이벤트 카테고리 연결
2. 할일: "회의 준비"
   - 투두 카테고리: 업무
   - 시작 시간: 14:00
   - 소요 시간: 60분
3. 타임라인에 계획 표시
4. 완료 시 자동으로 "① 업무" 이벤트 생성

## DB 마이그레이션

```bash
# Supabase SQL Editor에서 실행
supabase-migration-todo-categories.sql
```

또는 직접 데이터베이스에 적용:
```sql
-- 파일 내용을 Supabase 대시보드 > SQL Editor에 붙여넣고 실행
```

## 기술 구조

### 데이터베이스
- `todo_categories`: 투두 카테고리 테이블
  - id, name, event_category_id, created_at, updated_at
- `todos`: 기존 테이블에 컬럼 추가
  - todo_category_id, scheduled_time, duration

### API 엔드포인트
- `GET /api/todo-categories`: 투두 카테고리 목록
- `POST /api/todo-categories`: 투두 카테고리 생성
- `PATCH /api/todo-categories/:id`: 투두 카테고리 수정
- `DELETE /api/todo-categories/:id`: 투두 카테고리 삭제
- `POST /api/todos/:id/complete`: 투두 완료 및 이벤트 생성

### 주요 컴포넌트
- `TodoCategoryManager.jsx`: 투두 카테고리 관리 UI
- `TodoList.jsx`: 투두 카테고리 선택, 시간 입력 기능 추가
- `Timeline.jsx`: 투두 계획 표시 기능 추가
- `todo-categories-api.js`: 백엔드 API 핸들러
